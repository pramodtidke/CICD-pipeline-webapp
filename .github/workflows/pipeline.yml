name: CICD PIPELINE

on: 
    workflow_dispatch:

jobs:
    
   build and deploy:

        runs-on: ubuntu-latest

        steps:

            # Checkout code
            - name: checkout code
            uses: actions/checkout@v3

            # set up python

            - name: python setup
            